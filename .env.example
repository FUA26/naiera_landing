# ============================================
# Environment Variables Configuration
# ============================================
# This file documents all environment variables used in the application.
# Copy this file to .env.local and fill in the values.
#
# IMPORTANT: All environment variables are validated at build time using T3 Env.
# The application will refuse to build if required variables are missing or invalid.

# --------------------------------------------
# Development Environment
# --------------------------------------------
# Options: development | test | production
NODE_ENV=development

# --------------------------------------------
# Application Configuration (Client-side)
# --------------------------------------------
# These variables are exposed to the browser (prefixed with NEXT_PUBLIC_)

# Base URL of the application (must be a valid URL)
# Examples:
#   - Development: http://localhost:3000
#   - Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Application name (minimum 1 character)
NEXT_PUBLIC_APP_NAME="Ascodenote Boilerplate"

# n8n Chat Webhook URL (optional, must be a valid URL if provided)
# This enables the n8n chat widget on your site
# Example: https://your-n8n-instance.com/webhook/chat
# NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL=

# --------------------------------------------
# Optional: Skip Environment Validation
# --------------------------------------------
# Uncomment this to skip env validation (useful for Docker builds)
# WARNING: Only use this if you know what you're doing!
# SKIP_ENV_VALIDATION=true

# --------------------------------------------
# Add your custom environment variables below
# --------------------------------------------
# Example:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# API_KEY=your_api_key_here
